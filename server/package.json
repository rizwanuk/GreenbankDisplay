{
  "name": "greenbank-push-server",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "gen:vapid": "node -e \"(async()=>{const webpush=require('web-push');const v=webpush.generateVAPIDKeys();console.log('PUBLIC=',v.publicKey);console.log('PRIVATE=',v.privateKey)})()\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "web-push": "^3.6.6"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
